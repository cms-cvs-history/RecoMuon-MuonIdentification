# -*-SH-*-
process TEST = {
   include "RecoMuon/MuonIdentification/data/mergeMuons.cff"

   untracked PSet maxEvents = {untracked int32 input = -1}

   untracked PSet options =
   {
       untracked vstring Rethrow = {"ProductNotFound"}
   }

   source = PoolSource {
     untracked vstring fileNames = {
       "/store/RelVal/2007/6/30/RelVal-RelVal151Higgs-ZZ-4Mu-1183228327/0000/860585E7-5227-DC11-8227-000423D662EE.root"
     }
   }
   
   module Out = PoolOutputModule 
     {
	untracked string fileName = "/tmp/muonid.root"
	untracked vstring outputCommands =
	          {
		  "drop *",
		  "keep recoTracks_*_*_*",
		  "keep recoMuons_*_*_*",
		  "keep *_*_*_TEST"
		  }
     }
   
   path p = { mergeMuons }
   endpath outpath = { Out }
}


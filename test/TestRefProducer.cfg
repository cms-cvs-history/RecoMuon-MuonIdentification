# -*-SH-*-
process TEST = {
    untracked PSet maxEvents = {untracked int32 input = 100}

   untracked PSet options =
   {
       untracked vstring Rethrow = {"ProductNotFound"}
   }

   source = PoolSource {
     untracked vstring fileNames = {
       "file:/data/dmytro/tmp/506BC9B0-6527-DC11-9018-000423D644F6.root"
     }
     # untracked uint32 skipEvents = 409
   }
   
   include "RecoMuon/MuonIdentification/data/trackerMuonsLoose.cfi"
   
   module Out = PoolOutputModule 
     {
	untracked string fileName = "/tmp/muonrefs.root"
	untracked vstring outputCommands =
	          {
		  "drop *",
		  "keep recoTracks_*_*_*",
		  "keep recoMuons_*_*_*",
		  "keep *_*_*_TEST"
		  }
     }


   path p = {trackerMuonsLoose}
   endpath outpath = { Out }
   service = Timing { }
}

